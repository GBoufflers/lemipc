#include	"lemipc.h"

int		is_quitting(char *addr, char team, int pos)
{
  char		**map;
  int		i;
  int		j;
  int		k;

  i = 0;
  k = 0;
  map = malloc(6 * sizeof(char *));
  while (addr[i] != '\0')
    {

      i++;
    }
  return (0);
}
